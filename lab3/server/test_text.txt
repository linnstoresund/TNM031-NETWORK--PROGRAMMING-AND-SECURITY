What are the two characteristics of static packet filter firewalls?A static packet filter is one of the techniques for controlling the access to the network by analyzing the incoming and outgoing packages. It analyses them and either allows or drops them based on a set of if-then rules. The static packet filter firewalls perform packet filtering at the network/transport layer and examine the packages that try to traverse it